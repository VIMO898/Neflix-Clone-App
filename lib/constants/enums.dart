enum AllMediaType { movies, tvShows, people }

enum MovieCategory { popular, trending, topRated, nowPlaying, upcoming }

enum TvShowCategory { popular, trending, topRated, onTheAir, airingToday }

enum PersonCategory { popular, trending }

enum PeopleTrendingTimeWindow { day, week }

enum DefaultHomeScreen { discover, movies, tvShows }

enum MediaPosterSize { small, medium, large }

enum ExploreMediaType { movies, tvShows }

enum SortBy { popularity, rating, releaseDate }

enum SortOrder { ascending, descending }

enum MediaVideoType {
  trailer,
  teaser,
  bloopers,
  behindTheScenes,
  featurette,
  clip,
  openingCredits,
}

enum MediaImageType { backdrop, logo, poster }

enum GalleryMediaType { movie, tvShow }

enum MediaGridViewScreenType { withoutScaffold, withScaffold }

enum RenderDataType { load, available }

enum Gender { unknown, female, male, nonBinary }

enum ImageType { poster, backdrop, logo, profile, still }

enum ImageSize {
  w92,
  w154,
  w185,
  w300,
  w342,
  w500,
  w780,
  w1280,
  h632,
  original,
  w45,
}

enum MediaLibraryType { favorites, watchlist, alreadyWatchedlist }

enum DrawerNavigationType { push, pushReplacement }

enum SocialPlatform {
  freebaseMid,
  freebaseId,
  imdb,
  tvrage,
  wikidata,
  facebook,
  instagram,
  tiktok,
  twitter,
  youtube,
}

enum SelectableListViewType { region, language }
